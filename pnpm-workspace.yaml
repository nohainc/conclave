packages:
  - packages/*
  - apps/*
  # The transitional TypeScript Agent is archived; active host development is
  # exclusively apps/agent_app + apps/agent_engine.
  - '!apps/agent'

allowBuilds:
  esbuild: true
  workerd: true
