{
  "name": "conclave",
  "private": true,
  "packageManager": "pnpm@11.19.0",
  "scripts": {
    "build": "tsc -p packages/core/tsconfig.json && tsc -p packages/local-runtime/tsconfig.json && tsc -p apps/worker/tsconfig.json",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint .",
    "test": "vitest run packages/core/test packages/local-runtime/test apps/worker/test",
    "typecheck": "tsc -p packages/core/tsconfig.json && tsc -p packages/local-runtime/tsconfig.json && tsc -p apps/worker/tsconfig.json",
    "check": "prettier --check . && eslint . && tsc -p packages/core/tsconfig.json && tsc -p packages/local-runtime/tsconfig.json && tsc -p apps/worker/tsconfig.json && vitest run packages/core/test packages/local-runtime/test apps/worker/test"
  },
  "devDependencies": {
    "@eslint/js": "^9.35.0",
    "@types/node": "^24.5.2",
    "eslint": "^9.35.0",
    "prettier": "^3.6.2",
    "typescript": "^5.9.2",
    "typescript-eslint": "^8.44.0",
    "vitest": "^3.2.4",
    "wrangler": "^4.38.0"
  }
}
