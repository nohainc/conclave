name: conclave_plugin_protocol
description: JSON-RPC protocol shared by Conclave Agent Engine and Worker Plugins.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.5.0 <4.0.0"

dev_dependencies:
  test: ^1.25.0
