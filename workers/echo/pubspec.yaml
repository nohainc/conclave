name: conclave_echo_plugin
description: Deterministic Conclave Worker Plugin used for protocol and pipeline acceptance tests.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.5.0 <4.0.0"

dev_dependencies:
  test: ^1.25.0
