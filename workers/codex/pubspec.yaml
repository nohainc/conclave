name: conclave_codex_plugin
description: Codex CLI Worker Plugin adapter for Conclave Agent Engine.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.5.0 <4.0.0"

dev_dependencies:
  test: ^1.25.0
