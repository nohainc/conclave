{
  "name": "@conclave/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json",
    "check:startup": "wrangler check startup"
  },
  "dependencies": {
    "@conclave/core": "workspace:*",
    "@conclave/local-runtime": "workspace:*",
    "@conclave/orchestration": "workspace:*",
    "@conclave/persistence": "workspace:*",
    "@conclave/protocol": "workspace:*",
    "@conclave/providers": "workspace:*",
    "@conclave/security": "workspace:*"
  }
}
