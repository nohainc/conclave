---
interface Props {
  lines: string[];
  label?: string;
}

const { lines, label = "Architecture example" } = Astro.props;
---

<div class="terminal-panel" role="img" aria-label={label}>
  <code>{lines.join("\n")}</code>
</div>
