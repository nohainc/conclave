name: conclave_agent_engine
description: Headless Conclave AX Agent Engine.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  crypto: ^3.0.0
  conclave_protocol:
    path: ../../packages/dart/protocol

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
