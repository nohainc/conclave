name: conclave_host
description: Conclave AX Workspace desktop application and runtime.
publish_to: none
version: 1.0.3

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  crypto: ^3.0.0
  archive: ^4.3.0
  conclave_protocol:
    path: ../../packages/dart/protocol
  cryptography: ^2.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  test: ^1.25.0

flutter:
  uses-material-design: true
